name: WaffleMarket
options:
  deploymentTarget:
    iOS: "15.0"
  postGenCommand: pod install

targets:
  WaffleMarket:
    type: application
    platform: iOS
    sources:
      - path: WaffleMarket
    settings:
      CODE_SIGN_STYLE: Automatic
      INFOPLIST_FILE: WaffleMarket/Info.plist
      PRODUCT_BUNDLE_IDENTIFIER: com.wafflestudio.team8.WaffleMarket

      
